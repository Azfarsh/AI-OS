# Current quarter priorities

Update when focus shifts. `/agency-audit` reads this for Context scoring.

## Q focus (90 days)

1. Wire all six connections in `connections.md`
2. Run `/onboard-client` for every new signed client
3. Monthly `/report` for every active client (use `--send-email` when approved)
4. Weekly `/agency-audit` on Fridays

## Not doing this quarter

- Custom web dashboard
- Multi-tenant SaaS product
- Anything that requires a database or frontend
