services:
  ais-os:
    build: .
    stdin_open: true
    tty: true
    env_file:
      - .env
    volumes:
      - .:/app
      - ais-chroma:/app/memory/chroma
      - ais-sessions:/app/sessions
      - ais-logs:/app/logs
    working_dir: /app

volumes:
  ais-chroma:
  ais-sessions:
  ais-logs:
